'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _testUtils = require('react-dom/test-utils');

var _testUtils2 = _interopRequireDefault(_testUtils);

var _Header = require('./Header.driver');

var _Header2 = _interopRequireDefault(_Header);

var _driverFactory = require('wix-ui-test-utils/driver-factory');

var _Header3 = require('./Header');

var _Header4 = _interopRequireDefault(_Header3);

var _testkit = require('../../../testkit');

var _enzyme = require('../../../testkit/enzyme');

var _enzyme2 = require('enzyme');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Header', function () {
  var createDriver = (0, _driverFactory.createDriverFactory)(_Header2.default);

  it('should have a title', function () {
    var driver = createDriver(_react2.default.createElement(_Header4.default, { title: 'Header Title' }));
    expect(driver.title()).toBe('Header Title');
  });

  it('should have a subtitle', function () {
    var driver = createDriver(_react2.default.createElement(_Header4.default, { subtitle: 'Header Subtitle', title: 'Header Title' }));
    expect(driver.subtitle()).toBe('Header Subtitle');
  });

  describe('testkit', function () {
    it('should exist', function () {
      var div = document.createElement('div');
      var dataHook = 'myDataHook';
      var wrapper = div.appendChild(_testUtils2.default.renderIntoDocument(_react2.default.createElement(
        'div',
        null,
        _react2.default.createElement(_Header4.default, { title: 'Header Title', dataHook: dataHook })
      )));
      var headerTestkit = (0, _testkit.headerTestkitFactory)({ wrapper: wrapper, dataHook: dataHook });
      expect(headerTestkit.exists()).toBeTruthy();
    });
  });

  describe('enzyme testkit', function () {
    it('should exist', function () {
      var dataHook = 'myDataHook';
      var wrapper = (0, _enzyme2.mount)(_react2.default.createElement(_Header4.default, { title: 'Header title', dataHook: dataHook }));
      var headerDriverTestkit = (0, _enzyme.headerTestkitFactory)({ wrapper: wrapper, dataHook: dataHook });
      expect(headerDriverTestkit.exists()).toBeTruthy();
    });
  });
});