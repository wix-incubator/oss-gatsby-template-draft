'use strict';

var _eyes = require('eyes.it');

var _eyes2 = _interopRequireDefault(_eyes);

var _protractor = require('../../testkit/protractor');

var _storybookHelpers = require('../../test/utils/storybook-helpers');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Highlighter', function () {
  var storyUrl = (0, _storybookHelpers.getStoryUrl)('12. Other', 'Highlighter');
  var dataHook = 'story-highlighter';

  _eyes2.default.it('should render', function () {
    var driver = (0, _protractor.highlighterTestkitFactory)({ dataHook: dataHook });

    browser.get(storyUrl);

    expect(driver.getElement().isDisplayed()).toBe(true);
  });
});