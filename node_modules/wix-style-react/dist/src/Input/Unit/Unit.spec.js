'use strict';

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _Unit = require('./testkit/Unit');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('<Unit/>', function () {

  var createDriver = function createDriver(props) {
    return (0, _Unit.unitDriverFactory)((0, _Unit.componentFactory)(props));
  };

  it('should create a unit and render the given children', function () {
    var driver = createDriver({ children: _react2.default.createElement('div', { className: 'smth' }) });
    expect(driver.isEmpty()).toEqual(false);
    expect(driver.hasChild('.smth')).toEqual(true);
  });
});