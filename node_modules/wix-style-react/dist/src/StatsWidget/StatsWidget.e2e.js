'use strict';

var _eyes = require('eyes.it');

var _eyes2 = _interopRequireDefault(_eyes);

var _protractor = require('../../testkit/protractor');

var _protractor2 = require('wix-ui-test-utils/protractor');

var _storybookHelpers = require('../../test/utils/storybook-helpers');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('StatsWidget', function () {
  var storyUrl = (0, _storybookHelpers.getStoryUrl)('2. Layout', '2.7 StatsWidget');
  var dataHook = 'standard-stats-widget';

  _eyes2.default.it('should show proper amount of statistics', function () {
    var driver = (0, _protractor.statsWidgetTestkitFactory)({ dataHook: dataHook });

    browser.get(storyUrl);

    (0, _protractor2.waitForVisibilityOf)(driver.element(), 'Cannot find StatsWidget').then(function () {
      expect(driver.numberOfStatistics()).toBe(3);
    });
  });
});