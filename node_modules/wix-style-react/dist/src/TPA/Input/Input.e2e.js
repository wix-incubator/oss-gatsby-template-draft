'use strict';

var _eyes = require('eyes.it');

var _eyes2 = _interopRequireDefault(_eyes);

var _protractor = require('../../../testkit/protractor');

var _protractor2 = require('wix-ui-test-utils/protractor');

var _storybookHelpers = require('../../../test/utils/storybook-helpers');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('TPA Input', function () {
  var storyUrl = (0, _storybookHelpers.getStoryUrl)('TPA', 'Input');

  _eyes2.default.it('should enter value to Input', function () {
    var dataHook = 'story-input';
    var driver = (0, _protractor.tpaInputTestkitFactory)({ dataHook: dataHook });

    browser.get(storyUrl);

    (0, _protractor2.waitForVisibilityOf)(driver.element(), 'Cannot find Input').then(function () {
      driver.enterText('123');
      expect(driver.getText()).toBe('123');
    });
  });
});