'use strict';

var _eyes = require('eyes.it');

var _eyes2 = _interopRequireDefault(_eyes);

var _protractor = require('../../testkit/protractor');

var _protractor2 = require('wix-ui-test-utils/protractor');

var _storybookHelpers = require('../../test/utils/storybook-helpers');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Tabs', function () {

  var storyUrl = (0, _storybookHelpers.getStoryUrl)('6. Navigation', '6.3 Tabs');
  var dataHook = 'story-tabs';

  _eyes2.default.it('renders enough tab items', function () {
    var tabsDriver = (0, _protractor.tabsTestkitFactory)({ dataHook: dataHook });

    browser.get(storyUrl);

    return (0, _protractor2.waitForVisibilityOf)(tabsDriver.element()).then(function () {
      expect(tabsDriver.getItemsCount()).toBe(7);
    });
  });
});