'use strict';

var _pickAccessibilityProps = require('./pickAccessibilityProps');

describe('accessibility', function () {
  describe('pickAccessibilityProps', function () {
    it('should return only the aria props', function () {
      var props = {
        a: 1,
        'aria-label': 'hello'
      };
      expect((0, _pickAccessibilityProps.pickAccessibilityProps)(props)).toEqual({ 'aria-label': 'hello' });
    });
  });
});