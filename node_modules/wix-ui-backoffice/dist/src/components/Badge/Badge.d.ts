/// <reference types="react" />
import * as React from 'react';
import { Requireable } from 'prop-types';
import { BadgeProps as CoreBadgeProps } from 'wix-ui-core/StylableBadge';
import { Type, Skin } from './constants';
export interface BadgeProps {
    type?: Type;
    skin?: Skin;
    prefixIcon?: React.ReactElement<any>;
    suffixIcon?: React.ReactElement<any>;
    /** usually just text to be displayed */
    children: React.ReactNode;
}
export declare class Badge extends React.PureComponent<BadgeProps> {
    static displayName: string;
    static propTypes: {
        type: Requireable<any>;
        skin: Requireable<any>;
        prefixIcon: Requireable<any>;
        suffixIcon: Requireable<any>;
        children?: (object: CoreBadgeProps, key: string, componentName: string, ...rest: any[]) => Error;
        className?: (object: CoreBadgeProps, key: string, componentName: string, ...rest: any[]) => Error;
    };
    static defaultProps: {
        type: string;
        skin: string;
    };
    render(): JSX.Element;
}
