import { ButtonDriver as CoreButtonDriver } from 'wix-ui-core/dist/src/components/Button/Button.driver';
import { DriverFactory } from 'wix-ui-test-utils/driver-factory';
import { Skin, Priority, Size } from './constants';
export interface ButtonDriver extends CoreButtonDriver {
    getSkin: () => Skin;
    getPriority: () => Priority;
    getSize: () => Size;
}
export declare const buttonDriverFactory: DriverFactory<ButtonDriver>;
