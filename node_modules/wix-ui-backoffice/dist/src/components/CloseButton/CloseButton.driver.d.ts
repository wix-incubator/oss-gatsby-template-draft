import { ButtonDriver } from 'wix-ui-core/dist/src/components/Button/Button.driver';
import { ComponentFactory } from 'wix-ui-test-utils/driver-factory';
import { Skin, Size } from './constants';
export interface CloseButtonDriver extends ButtonDriver {
    getSkin: () => Skin;
    getSize: () => Size;
}
export declare const closeButtonDriverFactory: (factoryParams: ComponentFactory) => CloseButtonDriver;
