/// <reference types="react" />
import * as React from 'react';
import { Requireable } from 'prop-types';
import { BadgeProps as CoreBadgeProps } from 'wix-ui-core/Badge';
import { Skin } from './constants';
export interface CounterBadgeProps extends CoreBadgeProps {
    /** Type of the badge */
    skin?: Skin;
    /** Content of the badge */
    children?: any;
}
export declare class CounterBadge extends React.PureComponent<CounterBadgeProps> {
    static displayName: string;
    static propTypes: {
        skin: Requireable<any>;
        children: Requireable<any>;
    };
    static defaultProps: Partial<CounterBadgeProps>;
    render(): JSX.Element;
}
