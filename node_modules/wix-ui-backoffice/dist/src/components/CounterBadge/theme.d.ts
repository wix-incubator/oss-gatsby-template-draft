import { Skin } from './constants';
export declare const theme: ({ skin, canGrow }: {
    skin: Skin;
    canGrow: boolean;
}) => {
    border: string;
    height: string;
    lineHeight: string;
    minWidth: string;
    borderRadius: string;
    padding: string;
    backgroundColor: string;
    borderColor: string;
};
