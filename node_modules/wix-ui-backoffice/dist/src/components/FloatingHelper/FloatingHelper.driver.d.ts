import { DriverFactory } from 'wix-ui-test-utils/driver-factory';
import { ClosablePopoverDriver } from './ClosablePopover/ClosablePopover.driver';
import { FloatingHelperContentDriver } from '../../components/FloatingHelper/FloatingHelperContent/FloatingHelperContent.driver';
export interface FloatingHelperDriver extends ClosablePopoverDriver {
    /** Get the driver for the helper's content */
    getHelperContentDriver: () => FloatingHelperContentDriver;
    /** check wether the helper has a close button */
    hasCloseButton: () => boolean;
    /** click the close button */
    clickCloseButton: () => void;
    /** Get width of content's root element */
    getWidth: () => string;
}
export declare const floatingHelperDriverFactory: DriverFactory<FloatingHelperDriver>;
