/// <reference types="react" />
import * as React from 'react';
import { LinearProgressBarProps as CoreLinearProgressBarProps } from 'wix-ui-core/LinearProgressBar';
import { Omit } from '../../types/common';
export interface LinearProgressBarProps extends Omit<CoreLinearProgressBarProps, 'successIcon' | 'errorIcon'> {
    /** message to display when an error happens */
    errorMessage?: string;
    /** use light theme instead of dark theme */
    light?: boolean;
}
export declare const LinearProgressBar: React.SFC<LinearProgressBarProps>;
