/// <reference types="react" />
import * as React from 'react';
import { Requireable } from 'prop-types';
import { BadgeProps as CoreBadgeProps } from 'wix-ui-core/StylableBadge';
import { Skin } from './constants';
export declare type Content = string | number | React.ReactElement<any>;
export interface CounterBadgeProps {
    /** Skin of the badge */
    skin?: Skin;
    /** Content of the badge */
    children?: Content;
}
export declare class CounterBadge extends React.PureComponent<CounterBadgeProps> {
    static displayName: string;
    static propTypes: {
        skin: Requireable<any>;
        children: Requireable<any>;
        className?: (object: CoreBadgeProps, key: string, componentName: string, ...rest: any[]) => Error;
    };
    static defaultProps: CounterBadgeProps;
    render(): JSX.Element;
}
