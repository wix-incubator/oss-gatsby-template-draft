import { ComponentFactory } from 'wix-ui-test-utils/driver-factory';
import { TextDriver as CoreTextDriver } from '../core/CoreText/Text.driver';
import { Skin, Size } from './constants';
export interface TextDriver extends CoreTextDriver {
    getSize: () => Size;
    getSkin: () => Skin;
    isLight: () => boolean;
    isBold: () => boolean;
    isSecondary: () => boolean;
}
export declare const textDriverFactory: ({ element, eventTrigger, wrapper }: ComponentFactory) => TextDriver;
