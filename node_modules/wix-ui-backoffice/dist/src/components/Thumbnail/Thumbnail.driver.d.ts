import { ComponentFactory } from 'wix-ui-test-utils/driver-factory';
import { ThumbnailDriver as CoreThumbnailDriver } from 'wix-ui-core/dist/src/components/Thumbnail/Thumbnail.driver';
import { TextDriver } from '../Text/Text.driver';
export interface ThumbnailDriver extends CoreThumbnailDriver {
    titleDriver: () => TextDriver;
    getTitle: () => string;
    hasDescription: () => boolean;
    getDescription: () => string;
    hasImage: () => boolean;
    getImage: () => HTMLElement;
}
export declare const thumbnailDriverFactory: ({ element, eventTrigger, wrapper }: ComponentFactory) => ThumbnailDriver;
