export declare class IframesManager {
    private _iframeMap;
    private static getKey(apiKey, lang);
    addIframe(apiKey: string, lang: string): Window;
    getIframe(apiKey: string, lang: string): Window;
    hasIframe(apiKey: string, lang: string): boolean;
    removeAllIframes(): void;
    populateIframe(iframe: HTMLIFrameElement, apiKey: string, lang: string): void;
    private createInitializationScript();
    private createGoogleMapsScript(apiKey, lang);
}
