import { Input } from './Input';
export declare class InputDriver {
    root: HTMLElement;
    instance: Input;
    constructor(root: HTMLElement, instance: Input);
    readonly input: HTMLInputElement;
    readonly prefix: Node;
    readonly suffix: Node;
}
