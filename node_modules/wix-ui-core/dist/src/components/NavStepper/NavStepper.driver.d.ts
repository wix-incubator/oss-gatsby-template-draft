import { BaseDriver, DriverFactory } from 'wix-ui-test-utils/driver-factory';
export declare class NavStepperDriver implements BaseDriver {
    private readonly element;
    private styleUtil;
    private hasStyleState;
    private stepAt;
    constructor(element: Element);
    /**  returns the root element */
    readonly root: Element;
    /** checks if the stepper exists */
    exists(): boolean;
    /** checks if a step is active */
    isStepActive: (index: number) => boolean;
    /** checks if a step is disabled */
    isStepDisabled: (index: number) => boolean;
    /** checks if a step is visited */
    isStepVisited: (index: number) => boolean;
    /** returns text content of a step */
    stepContentAt: (index: number) => string;
    clickOnStep: (index: number) => void;
    /** returns the active step element */
    readonly activeStep: HTMLLIElement;
}
export declare const navStepperDriverFactory: DriverFactory<NavStepperDriver>;
