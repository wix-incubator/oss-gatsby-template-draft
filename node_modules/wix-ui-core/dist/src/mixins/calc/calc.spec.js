"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var calc = require("./index");
describe('calc mixin', function () {
    it('should calculate an expression with no units', function () {
        expect(calc('12 / 2')).toBe('6');
        expect(calc(' 12 / 2')).toBe('6');
        expect(calc('12/2')).toBe('6');
    });
    it('should infer the unit typed value when have values with some unit and some without units', function () {
        expect(calc('12% / 2')).toBe('6%');
        expect(calc('12 / 2%')).toBe('6%');
        expect(calc(' 12/ 2%')).toBe('6%');
    });
    it('should calculate an expression with same units', function () {
        expect(calc('12px / 2px')).toBe('6px');
        expect(calc('12% / 2%')).toBe('6%');
        expect(calc('12%/2%')).toBe('6%');
    });
    it('should return an expression with native calc function when there are various units', function () {
        expect(calc('12% / 2px')).toBe('calc(12% / 2px)');
    });
});
//# sourceMappingURL=calc.spec.js.map