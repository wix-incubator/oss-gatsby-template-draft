import * as React from 'react';
export interface EmailOpenProps extends React.SVGAttributes<SVGElement> {
    size?: string;
}
declare const EmailOpen: React.SFC<EmailOpenProps>;
export default EmailOpen;
