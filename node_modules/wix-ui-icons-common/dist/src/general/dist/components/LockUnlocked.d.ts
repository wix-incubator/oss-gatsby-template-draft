import * as React from 'react';
export interface LockUnlockedProps extends React.SVGAttributes<SVGElement> {
    size?: string;
}
declare const LockUnlocked: React.SFC<LockUnlockedProps>;
export default LockUnlocked;
