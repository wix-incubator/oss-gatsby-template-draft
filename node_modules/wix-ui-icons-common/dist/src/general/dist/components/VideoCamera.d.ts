import * as React from 'react';
export interface VideoCameraProps extends React.SVGAttributes<SVGElement> {
    size?: string;
}
declare const VideoCamera: React.SFC<VideoCameraProps>;
export default VideoCamera;
