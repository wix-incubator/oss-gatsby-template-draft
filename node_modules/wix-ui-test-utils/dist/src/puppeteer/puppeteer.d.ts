import { Page, ElementHandle } from 'puppeteer';
export declare function puppeteerTestkitFactoryCreator<T>(driverFactory: (e: ElementHandle | null, page: Page) => T): (obj: {
    dataHook: string;
    page: Page;
}) => Promise<T>;
